import styled from 'styled-components';

export const CustomDragLayerContainer = styled.div`
  height: 100%;
  width: 100%;
  z-index: 100;
  pointer-events: none;
  position: fixed;
  top: 0;
  left: 0;
`;
